export class Vote {
  id?: number;
  userId?: number;
  postId?: number;

  // constructor(id: number, userId: number, postId: number) {
  //   this.id = id;
  //   this.userId = userId;
  //   this.postId = postId;
  // }
}
