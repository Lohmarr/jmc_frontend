export class Comment {
  id?: number;
  commentText?: string;
  userId?: number;
  postId?: number;

  // constructor(id: number, commentText: string, userId: number, postId: number) {
  //   this.id = id;
  //   this.commentText = commentText;
  //   this.userId = userId;
  //   this.postId = postId;
  // }
}
